   <nav>
      <div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
        <div class="container-fluid">
          <%= link_to root_path do  %>
            <%= image_tag 'burgs_logo.png', alt: "burgs-r-us logo", class: "w-25" %>
          <% end %>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          <%# <% if user_signed_in? %>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <ul class="navbar-nav me-auto">
                <% unless current_user %>
                  <li class="nav-item">
                    <%= link_to "Create an account", new_user_registration_path, class: "btn btn-outline-info rounded-4 mx-1" %>
                  </li>

                  <li class="nav-item">
                    <%= link_to "Login",  new_user_session_path, class: "btn btn-outline-info rounded-4 mx-1" %>
                  </li>
                <% end %>

                <%#  ACTIONS AS LOGGED IN USER %>
                <% if current_user %>
                        <%= link_to "Add a burger", new_burger_path, class: "btn btn-outline-info rounded-4 mx-1" %>
                        <%= link_to "Your profile", profile_path(current_user.username), class: "btn btn-outline-info rounded-4 mx-1" %>
                        <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "btn btn-outline-info rounded-4 mx-1" %>

                <% end %>
                <!--li class="nav-item"-->
                <!--/li-->
              </ul>
            </div>
          <%# <% end %>
        </div>
      </div>
    </nav>
